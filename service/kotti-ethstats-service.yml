apiVersion: v1
kind: Service
metadata:
  name: kotti-stats
  namespace: ethercluster
  labels:
    app: kotti-stats
spec:
  type: LoadBalancer
  selector:
    app: kotti-stats
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
