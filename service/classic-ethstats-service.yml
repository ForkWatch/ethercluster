apiVersion: v1
kind: Service
metadata:
  name: classic-stats
  namespace: ethercluster
  labels:
    app: classic-stats
spec:
  type: LoadBalancer
  selector:
    app: classic-stats
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
