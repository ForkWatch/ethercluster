apiVersion: v1
kind: Service
metadata:
    labels:
        app: parity
    name: parity
    namespace: default
spec:
    selector:
        app: parity
    ports:
    - name: rpc-endpoint
      port: 8545
      protocol: TCP
      targetPort: 8545
    type: ClusterIP
